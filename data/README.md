# data

Datasets live here. This directory is gitignored, so nothing in it
gets committed except this README.

## Expected contents

- `raw/` — original downloaded files, never modified
- `interim/` — intermediate processing artifacts
- `processed/` — final inputs ready for modeling

Document the source and version of each dataset when you drop it in.
